{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://omp-deck.app/schemas/trigger-cron.json",
  "title": "RoutineTrigger — cron",
  "description": "Cron-scheduled trigger. Multiple cron triggers can be combined on one routine.",
  "type": "object",
  "required": ["cron"],
  "properties": {
    "cron": {
      "type": "string",
      "minLength": 1,
      "description": "Croner-compatible expression (5-field standard or 6-field with seconds)."
    }
  },
  "additionalProperties": false
}
